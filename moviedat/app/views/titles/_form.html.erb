<%= form_with(model: title, local: true) do |form| %>
  <% if title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(title.errors.count, "error") %> prohibited this title from being saved:</h2>

      <ul>
      <% title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cast %>
    <%= form.text_field :cast, id: :title_cast %>
  </div>

  <div class="field">
    <%= form.label :plat %>
    <%= form.text_area :plat, id: :title_plat %>
  </div>

  <div class="field">
    <%= form.label :movie_url %>
    <%= form.text_field :movie_url, id: :title_movie_url %>
  </div>

  <div class="field">
    <%= form.label :poster_url %>
    <%= form.text_field :poster_url, id: :title_poster_url %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
